class Beecrowd2

    def salary(n, w, a)#definindo as variáveis que seão utilizadas
        value = w * a
        puts "NUMBER = #{n}"
        puts "SALARY = U$ #{'%.2f' % value}" #sprintf já definido dentro do puts
    end
end